// Initialize Firebase using the configuration defined in firebase-config.js.
// firebase-config.js must be loaded before this script (see the pages that
// include it). Copy firebase-config.example.js to firebase-config.js first.
firebase.initializeApp(firebaseConfig);
firebase.analytics();
